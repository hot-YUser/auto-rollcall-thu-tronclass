# Base runtime dependencies — install with:  pip install -r requirements.txt
aiohttp>=3.9.5
PyYAML>=6.0.2
PyNaCl>=1.5.0
tomli>=2.0.1; python_version < "3.11"  # config.advanced.toml reader on Python 3.10 (3.11+ uses stdlib tomllib)

# Optional features live in extras (pip install -e .[<extra>]) and are not bundled
# in the default Windows release zip:
#   .[qr-image]   opencv-python-headless, Pillow  — decode QR from an image / clipboard screenshot
#   .[browser]    playwright                      — browser-assisted login (TKU auto, others opt-in)
#   .[packaging]  PyInstaller                     — build the Windows release zip
#   .[keyring]    keyring                         — store credentials in the OS keyring
# Clipboard QR auto-submit: image clipboard needs Pillow (.[qr-image]); text payloads use stdlib tkinter.
